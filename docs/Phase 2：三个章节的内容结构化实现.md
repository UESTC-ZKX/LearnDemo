请继续第二阶段，实现三大章节的“总览 + 分阶段详细描述”内容页。

内容要求：

第一章：大模型发展
- 总览：用时间线概括从规则/统计方法、RNN/LSTM、Transformer、GPT 系列、Tool Use/Agent 的演进
- 分阶段：
  1. 规则与统计方法
  2. RNN / LSTM
  3. Transformer
  4. GPT 时代
  5. Tool use / 长上下文 / Agent 过渡
- 每个阶段都要有：
  - 这个阶段的核心问题
  - 主要优化
  - 解决了什么问题
  - 仍存在哪些限制
  - 对应一个前端可视化演示区

第二章：Agent 框架选型
- 总览：先回答“什么时候需要 Agent，什么时候不需要”
- 分阶段：
  1. 单轮调用
  2. 多步骤 Workflow
  3. Tool 增强系统
  4. 高自治 Agent
- 每个阶段都要有：
  - 适用场景
  - 优点
  - 缺点
  - 工程复杂度
  - 一个适合讲解的可视化区域

第三章：Claude 工程实现解析
- 总览：说明目标是验证工业级系统如何实现 Prompt + Tool + Memory + Loop
- 分阶段：
  1. Prompt 结构
  2. Tool 调用机制
  3. 上下文管理
  4. Agent Loop
- 每个阶段都要有：
  - 模块说明
  - 输入输出
  - 关键设计思想
  - 可视化区域

实现要求：
- 先把文本结构和 UI 卡片布局做完整
- demo 区先允许用静态占位组件
- 所有章节内容尽量数据驱动，而不是把文案写死在 JSX 里